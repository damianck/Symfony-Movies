{% extends 'ShopMainBundle::layout.html.twig' %}

{% block content %}
    <div class="panel panel-default">
        <div class="panel-heading"><h4>Szczegóły filmu: <b>{{ movies.title }}</b> </h4></div>
        <div class="panel-body">
            <a href="{{ path('shop_main_movies') }}" class="btn btn-primary" role="button">Powrót</a>
        </div>
        {#<!-- Table -->#}

        <table   class="table table-striped table-bordered"   >
            <tr>
                {#<td><b>No.</b></td>#}
                <td><b>Tytuł</b></td>
                <td><b>Liczba sprzedaży</b></td>
                {#<td><b>Liczba recenzji</b></td>#}
                <td><b>Opis</b></td>
                <td><b>Cena</b></td>
                <td><b>Cover</b></td>
                <td><b>Stream</b></td>
                <td><b>Kategoria</b></td>
                <td><b>Aktor</b></td>
            </tr>
            {#<!-- wyswietlanie po id ?? -->#}

                <tr>
                    {#<td style="width: 70px">{{ loop.index }}</td>#}
                    {#<!-- Tablica movie -->#}
                    <td>{{ movies.title }}</td>
                    <td>{{ movies.numberOfSales }}</td>
                    {#<td>{{ movies.numberOfReviews }}</td>#}
                    <td>{{ movies.description }}</td>
                    <td>{{ movies.price }}</td>
                    <td>{{ movies.cover }}</td>
                    <td>{{ movies.stream }}</td>
                    {#<!-- category movie -->#}
                    <td>
                        {{ category }}
                    </td>
                    {#<!-- actors -->#}
                    <td>
                        {{ actors }}
                    </td>
                </tr>

        </table>

    </div>
{% endblock %}