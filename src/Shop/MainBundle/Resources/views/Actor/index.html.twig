{% extends 'ShopMainBundle::layout.html.twig' %}

{% block content %}
	<div class="panel panel-default">

		<div class="panel-heading"><h4>Lista wszystkich aktorów</h4></div>
		<div class="panel-body">
			<a href="{{ path('shop_main_actors_create') }}" class="btn btn-primary" role="button">Dodaj aktora</a>
		</div>


		{#<!-- Table -->#}
		<table   class="table table-striped table-bordered"   >

			<tr>

				<td><b>No.p</b></td>
				<td><b>Nazwisko</b></td>
				<td><b>Imie</b></td>
				<td><b>Kasowanie</b></td>

			</tr>
			{% for item in movies %}
				<tr>
					<td style="width: 70px">{{ loop.index }}</td>
					<td>{{ item.lastName }}</td>
					<td>{{ item.firstName }}</td>
					<td style="width: 80px">
						<a href="{{ path('shop_main_actors_delete', {'id': item.id}) }}" onClick="return confirm('Napewno chcesz usunać?')" class="btn btn-primary btn-sm" role="button">Skasuj</a>
					</td>
				</tr>
			{% endfor %}
		</table>
	</div>
{% endblock %}
